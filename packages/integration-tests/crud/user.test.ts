import { expect, it, describe } from "bun:test";

describe("User CRUD", () => {
  it("should be true", () => {
    expect(1).toBe(1);
  });

  it("should be false", () => {
    expect(0).toBe(0);
  });

  it("should be false", () => {
    expect(0).toBe(0);
  });

  it("should be false", () => {
    expect(0).toBe(0);
  });
});
